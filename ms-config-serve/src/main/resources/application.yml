#spring.application.name=ms-config-serve

server:
  port: 7070




spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DavidDevlo/DAD-2025-G2.git # ruta del repositorio git
          searchPaths: config-data
          default-label: main
security:
  user:
    name: david
    password: 61321351